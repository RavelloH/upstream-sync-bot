{
  "name": "upstream-sync-bot",
  "version": "0.1.0",
  "private": true,
  "description": "Template GitHub repository. Runs as a per-project sync bot that fans out upstream changes to every downstream fork that installed the paired GitHub App.",
  "type": "module",
  "scripts": {
    "enumerate": "node scripts/enumerate-installations.mjs"
  },
  "dependencies": {
    "@octokit/auth-app": "^7.1.4",
    "@octokit/request": "^9.1.3"
  },
  "engines": {
    "node": ">=20"
  }
}
